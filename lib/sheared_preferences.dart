import 'package:flutter/material.dart';

class ShearedPreferences extends StatelessWidget {
  const ShearedPreferences({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            Text("data"),
            Text("data"),
            Text("data"),
          ],
        ),
      ),
    );
  }
}